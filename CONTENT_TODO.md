# Content TODO

All placeholders that need real content before launch.

## Firm identity
- `[FIRM]` — firm name (in `lib/config.ts`)
- `[EMAIL]` — contact email (in `lib/config.ts`)
- `[LOCATION]` — office location (in `components/sections/Footer.tsx`)
- `[TAGLINE]` — firm tagline (in `lib/config.ts`)

## Hero (section 01)
- Trust logos — 5 `LogoSlot` components in `components/sections/Hero.tsx`

## Positioning (section 02)
- Body copy is directional — run through humanizer before launch

## Solutions (section 03)
All copy in `content/solutions.ts`:
- `001` description, detail, outcomes
- `002` description, detail, outcomes
- `003` description, detail, outcomes
- `004` description, detail, outcomes

## Agentic layer (section 04)
All copy in `content/agentic.ts`:
- `agent-governance` watches / does / returns
- `agent-portfolio` watches / does / returns
- `agent-deployment` watches / does / returns
- `agent-roi` watches / does / returns

## Approach (section 05)
All copy in `content/approach.ts`:
- Phase descriptions and activity lists for all 4 steps

## Proof / Outcomes (section 06)
- `[METRIC]` x4 — real figures with source attribution (`components/sections/Proof.tsx`)
- `[CASE STUDY TITLE]` — name of the engagement
- Case study body paragraph

## Contact page (Phase 5)
- Confirmation email / CRM integration in `app/api/contact/route.ts`

## Insights (Phase 7)
- MDX articles under `app/insights/`
